# CarrierSync VM - Scraper SII (ejecutar en la VM, no en Cloud Run)
# Incluye Selenium y ChromeDriver para automatización del portal SII

fastapi>=0.115.0
uvicorn[standard]==0.25.0
pydantic==2.5.0

# Automatización navegador
selenium>=4.15.0
webdriver-manager>=4.0.0

# 2Captcha (reCAPTCHA) - mismo uso que gestion_documental/verification_api
requests>=2.28.0
